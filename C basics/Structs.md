Structs are simply a way to group multiple fields or data points, and put them all into an `Object`.

__IMPORTANT:__ Structs are similar to Objects but they __DON'T__ have any behavior.
They are just Data.